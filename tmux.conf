unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

set -g allow-rename off
set -g mouse on

set -g base-index 1
setw -g pane-base-index 1

set -sg escape-time 1

# Smart pane switching with awareness of vim splits
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim='echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?x?)(diff)?$"'
bind -n C-h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
bind -n C-j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
bind -n C-k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
bind -n C-l if-shell "$is_vim" "send-keys C-l" "select-pane -R"
bind -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"

# Key bindings to resize panes
bind -r < resize-pane -L 10
bind -r > resize-pane -R 10
bind -r _ resize-pane -D 10
bind -r + resize-pane -U 10

# Open new panes at current directory
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -c "#{pane_current_path}"
unbind %
unbind '"'

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'seebi/tmux-colors-solarized'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-sessionist'
set -g @plugin 'tmux-plugins/tmux-sidebar'

# Plugin configurations

# Solarized colorscheme
set -g @colors-solarized $BACKGROUND

# Resurrect
set -g @resurrect-strategy-vim 'session'

# Sidebar
set -g @sidebar-tree-command 'tree -C'

# Initialize TMUX plugin manager
run '~/.tmux/plugins/tpm/tpm'

# Colorscheme modifications
set -g pane-active-border-fg cyan

# Status bar
set -g status-right-length 1000
set -g status-left-length 1000
set -g status-right '#(python3 ~/.velocity.py TMUXSTATUSRIGHT)'
set -g status-left '#(python3 ~/.velocity.py TMUXSTATUSLEFT)'
set -g window-status-format '[#I:#W]'
set -g window-status-current-format '[#I:#W]'
set -g window-status-style 'fg=yellow'
if-shell "echo $BACKGROUND | grep light" "set -g window-status-current-style 'fg=red,bg=brightwhite'" "set -g window-status-current-style 'fg=red,bg=brightblack'"

# Velocity
set -g @NOSPOTIFY false
set -g @NOBATTERY false
set -g @SHORTDATE false
set -g @NOSONGTICK false
set -g @STATUSRIGHTAUTOSCALE true
set -g @AUTOSCALECUTOFF 150
set -g @SPOTIFYFIELDLENGTH 20
